machine:
  node:
    version: 7.4.0
dependencies:
  override:
    - yarn
deployment:
  production:
    branch: master
    commands:
      - npm run build
      - surge --domain routes.barath.ga --project build
